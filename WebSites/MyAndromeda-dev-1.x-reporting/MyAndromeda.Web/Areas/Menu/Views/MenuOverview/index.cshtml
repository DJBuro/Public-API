@model dynamic
@{
    var context = Html.GetWorkContext();
    var chain = context.CurrentChain.Chain;
    var stores = chain.Stores;
}

@(Html.Kendo().Grid<MyAndromeda.Menus.Models.MenuStatus>().Name("Menu Status")
    .Columns(cols => {
        cols.Bound(e => e.ClientSiteName);

    })
)