@using System.Collections
@model object
           
@(
 Html.Kendo().DropDownListFor(m => m)
    .OptionLabel("Select a API")
    .DataValueField("Id")
    .DataTextField("Name")
    .DataSource(ds => {
        ds
            .Read(read => read.Action("ReadOnly", "ExternalApi"));
            
    })
)
