@{
    var workContext = Html.GetWorkContext();
    var route = workContext.CurrentRequest.RouteData.Route;
}
@using (Html.BeginHelpSection("Address", TabArea.Help,false)) 
{
    <div class="header">
        <h3>Address</h3>
    </div>
    <p>
        <a href="@Url.Action("ChangeAddress")">Navigate</a> to the editor page if you haven't already. The form is  a simple one 
    </p>
    <p>to-do: address</p>
    
}